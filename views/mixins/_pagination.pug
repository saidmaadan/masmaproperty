mixin pagination(page, pages, count)
  .pagination
    .pagination__prev
      if page > 1
        a(href=`/listings/page/${page - 1}`) Prev
    .pagination__text
      p page #{page} of #{pages} - #{count} total results
    .pagination__next
      if page < pages
        a(href=`/listings/page/${parseFloat(page) + 1}`) Next
